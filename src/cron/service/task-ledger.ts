/** Progress summary shown while a detached task ledger row represents an active cron run. */
export const CRON_TASK_RUNNING_PROGRESS_SUMMARY = "Running cron job.";

/** Main-session cron completion belongs to the heartbeat, not its scheduler receipt. */
export const CRON_HEARTBEAT_TASK_KIND = "cron-heartbeat";
